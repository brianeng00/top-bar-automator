"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var react_1 = __importStar(require("react"));
var Checkbox_1 = __importDefault(require("../Checkbox/Checkbox"));
var IndeterminateCheckbox_styles_1 = require("./IndeterminateCheckbox.styles");
var IndeterminateCheckbox = function (_a) {
    var label = _a.label, checkboxes = _a.checkboxes, dataQA = _a.dataQA, checked = _a.checked, className = _a.className, _b = _a.onChange, onChange = _b === void 0 ? function () { } : _b;
    var items = (0, react_1.useMemo)(function () {
        var itemsArray = [];
        checkboxes.forEach(function (child, index) {
            if (!child.disabled) {
                itemsArray.push(index);
            }
        });
        return itemsArray;
    }, [checkboxes]);
    var forLabel = typeof label === "string" ? label : "";
    var generateCheckedArray = function () {
        var checkedChildren = [];
        if (checked) {
            return items;
        }
        checkboxes.forEach(function (child, index) {
            if (child.checked && !child.disabled) {
                checkedChildren.push(index);
            }
        });
        return checkedChildren;
    };
    var _c = (0, react_1.useState)(generateCheckedArray()), checkedArray = _c[0], setChecked = _c[1];
    var handletoggle = function (value) {
        var currentIndex = checkedArray.indexOf(value);
        var newChecked = __spreadArray([], checkedArray, true);
        if (currentIndex === -1) {
            newChecked.push(value);
        }
        else {
            newChecked.splice(currentIndex, 1);
        }
        setChecked(newChecked);
    };
    var handleToggleAll = function () {
        if (checkedArray.length === items.length) {
            var childrenNotSelected = checkedArray.filter(function (value) { return items.indexOf(value) === -1; });
            setChecked(childrenNotSelected);
        }
        else {
            setChecked(items);
        }
    };
    var handleOnChange = function (event) {
        handleToggleAll();
        onChange(event);
    };
    return (react_1.default.createElement(IndeterminateCheckbox_styles_1.IndeterminateStylesDiv, { className: className },
        react_1.default.createElement(Checkbox_1.default, { label: label, dataQA: dataQA, checked: checkedArray.length === items.length && items.length !== 0, onChange: handleOnChange, isIndeterminate: checkedArray.length !== items.length && checkedArray.length !== 0 }),
        react_1.default.createElement(IndeterminateCheckbox_styles_1.IndeterminateStyleChildrenDiv, { role: "group", "aria-label": forLabel }, checkboxes.map(function (child, index) {
            var isChecked = child.disabled ? child.checked : checkedArray.indexOf(index) !== -1;
            return (react_1.default.createElement(Checkbox_1.default, { key: index, label: child.label, dataQA: child.dataQA, checked: isChecked, onChange: function (e) {
                    handletoggle(index);
                    return child.onChange && child.onChange(e);
                }, isIndeterminate: false, disabled: child.disabled, isInvalid: child.isInvalid }));
        }))));
};
IndeterminateCheckbox.displayName = "IndeterminateCheckbox";
exports.default = IndeterminateCheckbox;
//# sourceMappingURL=IndeterminateCheckbox.js.map