import React from "react";
import { PostProps } from "./Post.types";
declare const Post: {
    ({ dataQA, ariaLabel, padding, badgeIcon, badgeVariant, badgeText, header, body, link, firstImage, firstImageAltText, secondImage, secondImageAltText, mt, mb, mr, ml, m, className, }: PostProps): React.JSX.Element;
    displayName: string;
};
export default Post;
