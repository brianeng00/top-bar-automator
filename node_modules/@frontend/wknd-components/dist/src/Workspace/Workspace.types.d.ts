import { CSSProperties } from "react";
export interface StyledWorkspaceProps {
    zIndex?: CSSProperties["zIndex"];
    contentSize?: "withSidebar" | "fullScreen";
}
export interface WorkspaceProps extends StyledWorkspaceProps {
    dataQA: string;
    children?: React.ReactNode;
    className?: string;
}
