import { FC } from "react";
import { AlignPosition, SidePosition } from "../../DropDown";
interface RowsPerPageDropdownProps {
    align?: AlignPosition;
    ariaLabel: string;
    currentPage: number;
    dataQA: string;
    onChangeRowsPerPage: (currentRowsPerPage: number, currentPage: number) => void;
    rowOptions: Array<{
        value: number;
        label: string;
    }>;
    side?: SidePosition;
    rowsPerPage: number;
}
declare const RowsPerPageDropdown: FC<RowsPerPageDropdownProps>;
export default RowsPerPageDropdown;
