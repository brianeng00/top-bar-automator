import { FC } from "react";
import { Margins } from "../styleguide";
export interface LogoProps extends Margins {
    className?: string;
    title?: string;
    dataQA: string;
}
declare const Logo: FC<LogoProps>;
export default Logo;
