import { IconName } from "../Icon/Icon";
import { Margins } from "../styleguide";
type BadgeSizes = "small" | "default";
export type BadgeVariants = "default" | "active" | "testing" | "blue" | "error" | "warning";
export interface StyledBadgeProps extends Margins {
    variant?: BadgeVariants;
    size?: BadgeSizes;
}
export interface BadgeProps extends StyledBadgeProps {
    dataQA: string;
    text?: string;
    icon?: IconName;
    className?: string;
    isIconFilled?: boolean;
}
export {};
