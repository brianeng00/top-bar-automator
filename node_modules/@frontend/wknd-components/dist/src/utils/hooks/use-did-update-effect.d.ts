/**
 * A custom hook that is only triggers on updates, not on initial mount
 * @param fn
 * @param inputs
 */
export declare const useDidUpdateEffect: (fn: () => void, inputs: [string[] | string | undefined]) => void;
