import React, { FC } from "react";
export interface LayoutWrapperProps {
    dataQA: string;
    children?: React.ReactNode;
}
declare const LayoutWrapper: FC<LayoutWrapperProps>;
export default LayoutWrapper;
