import { InputBase } from "../Input";
import { Margins } from "../styleguide";
export interface StyledRadioProps {
    isInvalid?: boolean;
}
export interface RadioLabelProps {
    isDisabled?: boolean;
    isChecked?: boolean;
    isInvalid?: boolean;
}
export interface RadioProps extends StyledRadioProps, Margins, RadioLabelProps, InputBase {
    label?: string;
}
