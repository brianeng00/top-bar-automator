import React, { FC, ReactNode } from "react";
export interface LocalNavigationProps {
    dataQA: string;
    ariaLabel: string;
    className?: string;
    children?: React.ReactNode;
    header?: ReactNode;
}
declare const LocalNavigation: FC<LocalNavigationProps>;
export default LocalNavigation;
