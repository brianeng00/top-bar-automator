import React from "react";
export interface GlobalNavigationProps {
    dataQA: string;
    ariaLabel: string;
    className?: string;
    topActions?: React.ReactNode;
    navItems?: React.ReactNode;
    bottomActions?: React.ReactNode;
}
declare const GlobalNavigation: ({ dataQA, ariaLabel, className, topActions, navItems, bottomActions, }: GlobalNavigationProps) => React.JSX.Element;
export default GlobalNavigation;
