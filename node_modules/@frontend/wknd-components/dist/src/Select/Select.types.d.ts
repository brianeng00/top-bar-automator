import { SelectHTMLAttributes } from "react";
import { Margins } from "../styleguide";
type SelectValidation = "valid" | "invalid";
export interface SelectStyleProps {
    validation?: SelectValidation;
    placeholder?: string;
}
export interface SelectProps extends SelectHTMLAttributes<HTMLSelectElement>, Margins, SelectStyleProps {
    dataQA: string;
    label?: string;
    labelHtmlFor?: string;
    helperText?: string;
}
export {};
