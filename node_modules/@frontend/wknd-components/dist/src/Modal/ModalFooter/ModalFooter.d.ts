import React, { PropsWithChildren } from "react";
import { ModalFooterProps } from "../Modal.types";
declare const ModalFooter: {
    ({ primaryButtonText, primaryButtonOnClick, hidePrimaryButton, secondaryButtonText, hideSecondaryButton, dataQA, secondaryButtonOnClick, children, className, }: PropsWithChildren<ModalFooterProps>): React.JSX.Element;
    displayName: string;
};
export default ModalFooter;
