import React, { ReactNode } from "react";
import { ButtonGroupButtonProps } from "../ButtonGroup";
export interface TableActionsBarProps {
    selectedCountMessage?: string;
    /**
     * This is handled mainly in the DataTable but can be passed as a callback
     */
    handleDeselect?: () => void;
    tableBulkActions: ButtonGroupButtonProps[] | ReactNode;
}
export declare const TableActionsBar: ({ selectedCountMessage, handleDeselect, tableBulkActions, }: TableActionsBarProps) => React.JSX.Element;
