import React from "react";
import { Margins } from "../styleguide";
import { TableProps } from "./DataTable.types";
import { TableActionsBarProps } from "./TableActionsBar";
export type RowSpacing = "snug" | "default" | "loose";
export interface DataTableProps<T> extends TableProps<T>, Margins {
    dataQA: string;
    hasPagination?: boolean;
    isLoading?: boolean;
    noDataHeader?: string;
    noDataText?: string;
    noDataImage?: React.ReactNode;
    noFilterResultsImage?: React.ReactNode;
    noDataAction?: React.ReactNode;
    areRowsSelectable?: boolean;
    rowSpacing?: RowSpacing;
    tableActionsProps?: TableActionsBarProps;
}
declare const DataTable: {
    <T>({ dataQA, hasPagination, isLoading, noDataHeader, noDataText, noDataImage, noDataAction, areRowsSelectable, rowSpacing, tableActionsProps, onSelectedRowsChange, clearSelectedRows, subHeaderComponent, m, mt, mb, ml, mr, ...props }: DataTableProps<T>): React.JSX.Element;
    displayName: string;
};
export default DataTable;
