import React from "react";
import { DateObj, GetDatePropsOptions } from "dayzed";
import { StaticDatePickerProps } from "./DatePicker.types";
type DateButtonProps = Pick<StaticDatePickerProps, "dayzedProps" | "isRange"> & {
    hoveredDate: Date | null;
    dateObj: DateObj;
    dateProps: GetDatePropsOptions;
    tabIndex?: number;
};
export type RangePositions = "rangeStart" | "rangeEnd" | undefined;
export declare const DateButton: ({ hoveredDate, dateObj, dateProps, dayzedProps, isRange, tabIndex, }: DateButtonProps) => React.JSX.Element;
export {};
