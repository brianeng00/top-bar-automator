import React from "react";
import { DateRangePickerProps } from "./DateRangePicker";
export type DatePickerFooterProps = Pick<DateRangePickerProps, "dateFormat" | "placeholder"> & {
    applyOnClick: () => void;
    showFooterInputs?: boolean;
    startDate?: Date;
    endDate?: Date;
    handleStartDate: (date?: Date) => void;
    handleEndDate: (date?: Date) => void;
    minDate?: Date;
    maxDate?: Date;
};
declare const DatePickerFooter: ({ applyOnClick, showFooterInputs, startDate, endDate, handleStartDate, handleEndDate, dateFormat, placeholder, minDate, maxDate, }: DatePickerFooterProps) => React.JSX.Element;
export default DatePickerFooter;
