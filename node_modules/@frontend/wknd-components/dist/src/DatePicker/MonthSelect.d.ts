import React from "react";
type MonthSelectProps = {
    dataQA: string;
    month: number;
    calendarIndex: number;
    setDate: (newDate: Date) => void;
    monthNames: string[];
    calendarOffset: number;
    setCalendarOffset: (newOffset: number) => void;
    currentSelectedYear: number;
    minDate: Date;
    maxDate: Date;
};
declare const MonthSelect: ({ dataQA, month, monthNames, calendarIndex, setDate, calendarOffset, setCalendarOffset, currentSelectedYear, minDate, maxDate, }: MonthSelectProps) => React.JSX.Element;
export default MonthSelect;
