import { Shortcut } from "./DatePicker.types";
export declare const getDefaultDates: () => {
    defaultMinDate: Date;
    defaultMaxDate: Date;
};
export declare const today: Date;
/**
 * NOTE: If a change is made here, make sure it gets replicated in `__mocks__/DatePicker.defaults`
 */
export declare const defaultShortcutsList: {
    today: {
        label: string;
        getDates: () => Date[];
        id: string;
    };
    yesterday: {
        label: string;
        getDates: () => Date[];
        id: string;
    };
    lastSevenDays: {
        label: string;
        getDates: () => Date[];
        id: string;
    };
    lastThirtyDays: {
        label: string;
        getDates: () => Date[];
        id: string;
    };
    lastNinetyDays: {
        label: string;
        getDates: () => Date[];
        id: string;
    };
    lastMonth: {
        label: string;
        getDates: () => Date[];
        id: string;
    };
    lastQuarter: {
        label: string;
        getDates: () => Date[];
        id: string;
    };
    yearToDate: {
        label: string;
        getDates: () => Date[];
        id: string;
    };
};
export declare const DEFAULT_SHORTCUTS: Shortcut[];
