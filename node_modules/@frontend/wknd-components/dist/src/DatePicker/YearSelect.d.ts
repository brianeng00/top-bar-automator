import React from "react";
type YearSelectProps = {
    dataQA: string;
    currentSelectedYear: number;
    minYear: number;
    maxYear: number;
    month: number;
    setDate: (newDate: Date) => void;
    calendarOffset: number;
    setCalendarOffset: (newOffset: number) => void;
    calendarIndex: number;
};
declare const YearSelect: ({ currentSelectedYear, dataQA, minYear, maxYear, month, setDate, calendarOffset, setCalendarOffset, calendarIndex, }: YearSelectProps) => React.JSX.Element;
export default YearSelect;
