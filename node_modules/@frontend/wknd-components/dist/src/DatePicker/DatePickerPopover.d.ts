import React, { ReactNode } from "react";
import { PopoverMenuProps } from "../Popover";
import { StyledPopoverProps } from "../Popover/Popover.types";
import { StaticDatePickerProps } from "./DatePicker.types";
import { DatePickerInputProps } from "./DatePickerInput";
import { DateRangePickerInputProps } from "./DateRangePickerInput";
type PickerPopoverExtendedProps = Pick<StaticDatePickerProps, "isRange"> & Pick<DatePickerInputProps, "label" | "dateFormat" | "placeholder" | "inputLeftIcon" | "inputRightIcon"> & Pick<DateRangePickerInputProps, "inputLeftIcon" | "inputRightIcon"> & Pick<StyledPopoverProps, "$zIndex">;
export type DatePickerPopoverProps = PickerPopoverExtendedProps & {
    dataQA: string;
    children?: ReactNode | JSX.Element[];
    datesSelected?: StaticDatePickerProps["dayzedProps"]["selected"];
    /**
     * @description closes the Popover when a date is selected
     * @default true
     */
    closeOnDateSelected?: boolean;
    onDateChange?: (date?: Date | Date[]) => void;
    /**
     * @description trigger passed to the Popover which displays the DatePicker and DateRangePicker
     * @default <DatePickerInput/> for the single DatePicker
     * @default <DateRangePickerInput/> for the DateRangePicker
     */
    trigger?: PopoverMenuProps["trigger"];
    minDate?: Date;
    maxDate?: Date;
};
declare const DatePickerPopover: ({ children, trigger, dataQA, closeOnDateSelected, datesSelected, onDateChange, isRange, label, dateFormat, placeholder, minDate, maxDate, inputLeftIcon, inputRightIcon, $zIndex, }: DatePickerPopoverProps) => React.JSX.Element;
export default DatePickerPopover;
