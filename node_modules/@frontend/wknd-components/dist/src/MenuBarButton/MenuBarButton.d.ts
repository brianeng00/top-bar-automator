import React from "react";
import { ButtonBase } from "../Button/Button.types";
import { Margins } from "../styleguide";
export interface MenuBarButtonProps extends Margins, ButtonBase {
    buttonText?: string;
    avatarText?: string;
    imageSource?: string;
}
declare const MenuBarButton: React.ForwardRefExoticComponent<MenuBarButtonProps & React.RefAttributes<HTMLButtonElement>>;
export default MenuBarButton;
