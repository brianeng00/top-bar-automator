import React, { ReactNode } from "react";
type DrawerDescriptionProps = {
    children?: ReactNode;
};
declare const DrawerDescription: ({ children, }: DrawerDescriptionProps) => React.JSX.Element;
export default DrawerDescription;
