import React, { ReactNode } from "react";
type DrawerTitleProps = {
    children?: ReactNode;
    className?: string;
};
declare const DrawerTitle: ({ children, className, }: DrawerTitleProps) => React.JSX.Element;
export default DrawerTitle;
