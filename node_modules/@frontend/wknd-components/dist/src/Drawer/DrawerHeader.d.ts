import React, { ReactNode } from "react";
type DrawerHeaderProps = {
    children?: ReactNode;
    className?: string;
};
declare const DrawerHeader: ({ children, className, }: DrawerHeaderProps) => React.JSX.Element;
export default DrawerHeader;
