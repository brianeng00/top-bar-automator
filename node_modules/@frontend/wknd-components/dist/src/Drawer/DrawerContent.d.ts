import React, { ReactNode } from "react";
type DrawerContentProps = {
    children?: ReactNode;
    className?: string;
};
declare const DrawerContent: ({ children, className, }: DrawerContentProps) => React.JSX.Element;
export default DrawerContent;
