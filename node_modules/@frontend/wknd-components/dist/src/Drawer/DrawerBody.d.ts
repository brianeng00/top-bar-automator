import React, { ReactNode } from "react";
type DrawerBodyProps = {
    children?: ReactNode;
    className?: string;
};
declare const DrawerBody: ({ children, className, }: DrawerBodyProps) => React.JSX.Element;
export default DrawerBody;
