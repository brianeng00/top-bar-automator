import React from "react";
import { GroupBase } from "react-select";
import { ComboboxProps, OptionType } from "./Combobox.types";
/** @description Combobox is built on top of `react-select`
 * @see [`react-select`](https://react-select.com/home) for more information
 */
declare const Combobox: {
    <Option extends OptionType, IsMulti extends boolean = false, Group extends GroupBase<Option> = GroupBase<Option>>({ dataQA, placeholder, label, labelHtmlFor, id, helperText, options, isMulti, comboboxIcon, comboboxRef, required, styles, validation, ...props }: ComboboxProps<Option, IsMulti, Group>): React.JSX.Element;
    displayName: string;
};
export default Combobox;
